import React from 'react'
import styles from './page.module.css'

export default function page() {
  return (
    <div className={`${styles.container}`}>
      <h1 className={`${styles.title}`}>犬</h1>
    </div>
  )
}
